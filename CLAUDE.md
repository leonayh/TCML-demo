# 專案持久指示 — 【僑委會】官網改版

## 元件一致性(最高優先)
- 新增任何功能元件時,**優先抓取現有 design system / 既有頁面中的同功能元件**,沿用其樣式、尺寸、顏色、圓角、間距與互動狀態。
- 目的:避免「全站相同功能卻有不同樣式」。例如輪播進度條、按鈕、卡片、搜尋列、標籤等,務必全站統一。
- **不確定要用哪個現有元件、或現有元件是否適用時,先詢問使用者,不要自行發明新樣式。**
- 參考來源優先序:① 既有 TCML 頁面(如 TCML Mobile Home 內的 design system 元件)→ ② Figma → ③ 既有 prototype(screens.jsx / styles.css)。
- 動態元件(如 data-stack-dots 輪播指示器)要以「實際 render 出來的狀態」為準(例如 active 狀態是 20px 寬的圓角長條,而非靜態 markup 的 7px)。

## 字體與字重規範(Typography)
- 字體只用兩套:**Montserrat**(`--fdisp`,標題用)、**Outfit**(`--fui`,內文/UI 用);中文 fallback 為 **Noto Sans TC**。
- **字重一律使用 400(Regular)** — 不使用 500/600/700。標題與內文皆 400,層級改用「字級大小」區分,不靠字重。
  - 唯一例外:輔助/說明性小字(副標、頁尾敘述、英文小字)可用 **300(Light)**。
- 階層用字級(目前的尺度,可再調整):主標 ~46px → 段落標題 40px → 卡片標題 ~23px → 內文 16–18px → 小字 12–15px。

